# MEMORY.md — Current State at a Glance

Last updated: 2026-09-20 (later session). Committed and pushed through `e72ba0c`; working tree has further uncommitted changes on top (see below) — not yet committed, per "never commit unless asked."

This is the 30-second version of "where are we." For full history and the reasoning behind every decision, see **`PROGRESS.md`**. For how to work in this repo (conventions, hard rules, known gotchas), see **`CLAUDE.md`**.

## Where we are

- The site is built and functionally complete against the original ~40-section brief: every page/route, SSR + full prerendering (23 static routes), dark theme, accessibility-audited (WCAG AA contrast verified), unit-tested (Vitest) and e2e-tested (Playwright), CI configured (lint → test → build → e2e on push/PR).
- A full **light theme + header toggle** was added on top of the original dark-only design — independently WCAG-verified, not an inverted palette. (`PROGRESS.md` §15)
- The `/work` portfolio was enriched using independently-verified GitHub repository analyses (real clone/build/test, not README-trust) — added 3 new real personal projects, upgraded 2 existing ones with real GitHub links and verified technical detail, and deliberately excluded one project the analysis found genuinely broken. (`PROGRESS.md` §3/§4)
- Real branding (logo mark, full favicon set) and real founder headshots have replaced all placeholder/initials assets. (`PROGRESS.md` §13/§14)
- `/work` now lists **7 projects** (down from 10 — `personal-portfolio`, `enterprise-data-pipelines`, and `full-stack-product-applications` were removed at the user's request 2026-09-20; the latter two were the site's *only* `professional-experience` projects, so that ownership category currently has zero entries — the labeling logic itself still exists and is tested directly, not content-dependently). 3 of the 7 show a real screenshot (`enterprise-micro-frontend-shell`'s was explicitly removed per the same request, project entry kept) — the rest keep the gradient placeholder, either because no verifiable public repo/demo exists (`multi-tenant-hospital-management-system`) or (`flowmedic`) because it needs Docker, which this machine doesn't have. (`PROGRESS.md` §18/§19)
- A **premium motion/interaction system** was added on top of the existing (already-decent) design: card hover lift+glow, a pointer-reactive hero ambient glow, animated nav underline, a traveling pulse on the Process section's connector line, and trailing-arrow hover nudges on every CTA — all pure CSS + one new small directive, zero new npm dependencies, fully `prefers-reduced-motion`-aware. Full writeup: `docs/portfolio-enhancement-report.md` (and the pre-implementation audit at `docs/portfolio-motion-audit.md`).
- **Both founder pages** were rephrased and differentiated 2026-09-20 (Kunal: frontend/product/architecture/AI-assisted engineering; Mrityunjay: backend/cloud/data/infrastructure/reliability) — `Founder.expertise` is now `{title, description}` pairs instead of flat tags, and two new sections were added to the shared template: "Role at letusdeliver" and "Building letusdeliver." Full writeup: `docs/founder-profile-update.md`.
- Repo: `https://github.com/letusDeliver/letusDeliver-portfolio`, branch `main`.

## What's genuinely still open (not just "could be nice")

1. **`flowmedic` has no screenshot and no Docker/WSL path to get one on this machine.** The user explicitly chose to skip it (2026-09-20) rather than install Docker Desktop. Revisit if they set up Docker/WSL themselves, or want to supply their own screenshot. Full detail: `PROGRESS.md` §19.
2. **No `professional-experience` projects currently exist on `/work`.** All 3 that ever existed (Nightwatch Platform, Enterprise Data Pipelines, Full-Stack Product Applications) were removed at explicit user request across two sessions. The `ownershipLabel`/`contextLabel` machinery that displays this category is untouched and still correct — it's just unexercised by current content. Don't treat the empty category as a bug to "fix" by re-adding invented content; only re-add if the user provides another real prior-employment project.
3. **No real letusdeliver-owned client work exists yet** — the company is new. `/work` only shows personal projects and clearly-labeled prior-employment work. Do not invent client work, testimonials, or metrics under any circumstance.
4. **Placeholders waiting on real input from the user, not code work**: social media URLs (`site.config.ts`, all still `null`), insights articles (currently 3 "coming soon"), privacy/terms legal copy (placeholder, pending real legal review), and a chosen deployment/hosting target (no deploy step in CI yet). Full list: `PROGRESS.md` §11.
5. **Contact form backend is real in dev, still mocked in production.** A real Django backend now exists and is wired up for local dev (`environment.ts` → `http://127.0.0.1:8010/api/contact/submissions/`, `useMockContactApi: false`) — verified end-to-end against the running backend. `environment.production.ts` is still `useMockContactApi: true` because `api.letusdeliver.com` isn't a deployed backend yet; flip that once it is. Full detail: `PROGRESS.md` §17.
6. **`ibkr-webapp-main`'s secret was reported rotated by the user (2026-09-20) but the repo's git history is unchanged** — the old `flask_secret_key` value is still literally committed in `config.ini` on `main`. The link is now live on the site per the user's explicit confirmation, but if this ever matters again (e.g. someone asks "is that repo safe to share"), the honest answer is "the live value was rotated, the git history was not purged." Full detail: `PROGRESS.md` §4a's 2026-09-20 update.
7. **Kunal's years-of-experience figure**: a later content brief stated "5+ years"; the resume-sourced figure already in `founders.data.ts` is "4.5+ years." Kept the verified figure per CLAUDE.md §2 rather than the brief's rounder number — confirm with Kunal if 5+ is now accurate and it's a one-line change. Full detail: `PROGRESS.md` §22.

- **Both founder pages rephrased & differentiated** (not yet committed — `docs/founder-profile-update.md`, `PROGRESS.md` §22): resolves the open item below (was §21's flagged gap) — this brief explicitly asked for "Role at letusdeliver" and "Building letusdeliver" as page sections, so the `Founder` model was extended (`expertise` → `{title, description}` pairs; new `roleAtLetusdeliver`/`buildingLetusdeliver` fields) and both new sections were added to the one shared `founder.html` template. Also generalized away the internal platform name ("Nightwatch") from Mrityunjay's Insight Global bullet — confidential-client-info policy extended beyond just the client's own name. CTA on both pages changed from founder-named to company-oriented ("Have a product idea?").
- **Mrityunjay's founder profile refreshed from an updated bio the user supplied directly** (`PROGRESS.md` §21): role, summary, philosophy, expertise tags, technical stack (added a new "AI & Engineering Tools" category, matching Kunal's page), and all 3 experience entries (Insight Global's highlights rewritten to match the new source more closely — and the "— Client: The Home Depot" qualifier dropped, since the new source doesn't include it; AdGlobal360 needed no changes, it already matched; Toxsl gained one additional highlight).
- **Footer design follow-up** (not yet committed — `PROGRESS.md` §20a): the layout fix below solved the structural problem, but the user asked again to make the footer "awesome" — it was correct but visually flat, disconnected from the premium motion language the rest of the site just gained. Added an ambient accent glow (reusing the hero's exact visual pattern) + a gradient hairline on the top border, gave `Site`/`Elsewhere` links the same scale-in hover underline the header nav uses (legal links in the bottom row deliberately kept plain, for hierarchy), enlarged the logo, and added the real `siteConfig.tagline` ("Think. Build. Deliver.") to the brand column. Deliberately did *not* add a repeated "Start a Project" CTA — `final-cta` right above the footer already ends in one.
- **Footer redesign + 3 more project removals + 1 screenshot removal** (not yet committed): the footer's `md:grid-cols-[2fr_1fr_1fr]` layout left a large dead gap because the "Elsewhere" social column was always rendering 7 dead "coming soon" placeholders (`site.config.ts`'s social URLs are all still `null`) that looked like real links. Fixed by filtering the footer's social links to only real (non-null) URLs — the whole column now simply doesn't render when none exist — and switching the layout from a wide 3-column grid to a content-hugging left-aligned flex row (brand block + clustered nav links), which reads as intentional minimalism instead of a layout bug regardless of how many columns end up present. Also removed, per explicit user request: the `enterprise-micro-frontend-shell` project's screenshot (kept the project entry itself), and the `personal-portfolio`, `enterprise-data-pipelines`, and `full-stack-product-applications` project entries entirely. The latter two were the site's only `professional-experience` projects — see open item 2 above. Cleaned up every downstream reference (founders' `projectSlugs`, `sitemap.xml`, the e2e test that used `enterprise-data-pipelines` as its professional-experience example, and a unit test that asserted a professional-experience project exists in the live data — rewritten to test the `ownershipLabel` mapping function directly instead of depending on content that may or may not exist). `/work` now lists 7 projects, 3 with real screenshots; build prerenders 20 routes (was 23).
- **Premium motion & AI-inspired interaction system** (not yet committed — `docs/portfolio-motion-audit.md` + `docs/portfolio-enhancement-report.md`): a full audit-then-approve-then-implement pass (user explicitly required the audit doc + sign-off before any code changed). Added motion design tokens, a `PointerGlow` directive (rAF-throttled, zoneless-safe — writes raw DOM styles, never a signal), card lift/glow on every card grid site-wide, a pointer-reactive hero ambient glow (extending the existing pipeline-diagram motif rather than adding a redundant new one), an animated nav-link underline, a mobile-menu entrance animation, a traveling pulse on the Process section connector, and trailing-arrow hover nudges on ~20 CTAs across the site (via a CSS utility class, not a Button-component change). Also fixed a real inconsistency found along the way: the homepage's "Featured work" preview now shows real project screenshots instead of a generic placeholder. Zero new npm dependencies. `ng lint`/`test`/`build` clean throughout; e2e 12/13 (1 pre-existing failure, unrelated — needs the local contact-form backend running).
- **Added `CareerNaukri` (new project) + a screenshot for the previously-withheld `ibkr-signal-scanner`** (not yet committed — `PROGRESS.md` §19): researched and verified a brand-new Django project from scratch (no prior GitHub analysis existed for it) before writing any site copy — real model/view inspection, not guesses; explicitly avoided claiming things that turned out false (e.g. no real test suite exists, despite `tests.py` files being present). Confirmed with the user first that `ibkr-webapp-main`'s committed secret had been rotated (see open item 5 below for a caveat on that) before adding its `githubUrl` + a real screenshot. `flowmedic` was asked about and explicitly skipped by the user (no Docker here). Also fixed a real `NG0913` perf warning (lazy-loaded LCP image) found while doing this. `/work` now lists 10 projects, 5 with real images; build prerenders 23 routes.
- **Removed the "Nightwatch Platform" project** (not yet committed) — user-requested removal from `/work`. Deleted the entry from `projects.data.ts`, its slug from Kunal's `projectSlugs` in `founders.data.ts`, its URL from `sitemap.xml`, and repointed the e2e test that used it as its professional-experience example to `enterprise-data-pipelines` instead. Left the founder-bio resume prose that mentions Nightwatch by name untouched — that's a factual employment-history claim (§2 sourcing), not the project listing the user asked to remove.
- **Real project screenshots on `/work`** (not yet committed — `PROGRESS.md` §18): cloned, built and ran 3 of the 4 publicly-linked personal projects locally (skipped `flowmedic`, needs Docker which this machine doesn't have) and screenshotted the actual running apps — no stock photos or mockups. Added `imageUrl`/`imageAlt` to the `Project` model, wired into both `/work` cards and detail-page heroes + `og:image`.
- **Contact form: wired to the real backend + 4 follow-up UX fixes** (both same day — `PROGRESS.md` §17/§17a, commit `e72ba0c`, pushed): swapped the mock for the real Django backend in dev (snake_case field mapping verified against the backend's own DRF `OPTIONS` metadata, not guessed; real success/400/429 handling; per-field backend error display), then fixed double-submission, no way to remove a chosen attachment, no success toast (new `shared/ui/toast` component), and fields accepting leading/trailing whitespace. `ng test` 13 → 16, all passing.
- Fixed a webhint "no-inline-styles" false-flag on the hero SVG gradient (moved `stop-color` out of an inline `style=` attribute into a CSS class) — commit `d8dde8a`.
- Added the dark/light theme switcher (`ThemeService`, header toggle, dedicated `--color-on-accent` token, fixed a latent theme-coupling bug in the primary button) — commit `058d663`.
- Enlarged and restructured the founder cards (home + `/about`) to an image-left/description-right flex layout; enlarged the founder detail hero photo.
- Added real founder profile photos, replacing initials avatars, across 3 templates + wired into `og:image`/Person structured data.
- Added real logo mark + full favicon set to header/footer/`index.html`.
- Portfolio enrichment from GitHub analysis — commit `5160c69`.

## How to pick this back up cold

```bash
cd D:\Start-up\letusdeliver
npm install   # if node_modules isn't present
npm start     # dev server -> http://localhost:4200
```

The contact form's real backend is a separate service, not started by anything above — see `CLAUDE.md`'s "Contact form backend" section before assuming it's reachable.

Read `CLAUDE.md` for the rules of the road, then jump into the relevant `PROGRESS.md` section for whatever you're about to touch. Don't assume — re-read the section, the reasoning there is often load-bearing.
